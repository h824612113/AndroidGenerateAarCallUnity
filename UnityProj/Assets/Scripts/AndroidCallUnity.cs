using UnityEngine;
using UnityEngine.UI;

public class AndroidCallUnity : MonoBehaviour
{
    public Text textACU;

    public void TestAndroidCallUnity(string str)
    {
        
        textACU.text = str;
    }
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
